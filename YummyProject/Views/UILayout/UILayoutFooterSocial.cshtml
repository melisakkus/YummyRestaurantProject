@model List<YummyProject.Models.RestaurantSocial>


<div class="col-lg-3 col-md-6">
    <h4>Sosyal Medya</h4>

    <div class="social-links d-flex">
        @foreach (var item in Model)
        {
            <a href="@item.Url"><img src="@item.Icon" style="width:30px ; height:30px" /></a> 
        }
    </div>

</div>